{
  "name": "vault-ops-orphan-finder",
  "version": "0.1.1",
  "private": true,
  "description": "Vault Ops - Orphan Finder handles unlinked note detection with scheduled scan and delivers orphan notes list as command output.",
  "license": "MIT",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "test": "node --test",
    "package": "bash ./scripts/package.sh"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "obsidian": "^1.6.7",
    "typescript": "^5.7.3",
    "@types/node": "^22.10.2"
  }
}
